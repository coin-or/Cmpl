%data knapsack-data.cdat : boxes set, w[boxes], p[boxes], maxWeight, minSales
%display nonZeros

variables:
	x[boxes] : binary;
objectives:
	sales: p[]T * x[] ->max;
constraints:
	weight: w[]T * x[] <= maxWeight;
 
 
 
 
 
 
